This notebook explores the concept of convolution in both 1D and 2D. It includes smoothing of sine waves using rectangular pulses, visualizations of signals in the time and frequency domains, and edge detection in images using 2D convolution with the Sobel filter. The notebook is self-contained and will load a default image if no input image is found. You can run all cells in order and experiment with different filters to better understand convolution and its applications.
